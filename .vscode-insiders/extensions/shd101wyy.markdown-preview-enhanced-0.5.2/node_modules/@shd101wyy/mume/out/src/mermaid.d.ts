/**
 * A wrapper of mermaid CLI
 * http://knsv.github.io/mermaid/#mermaid-cli
 * But it doesn't work well
 */
export declare function mermaidToPNG(mermaidCode: string, pngFilePath: string, css?: string): Promise<string>;
